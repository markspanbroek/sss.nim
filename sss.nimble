# Package

version     = "0.1.1"
author      = "Mark Spanbroek"
description = "Shamir secret sharing"
license     = "MIT"

# Dependencies

requires "nim >= 1.0.6 & < 2.0.0"
requires "nimterop >= 0.4.4 & < 0.5.0"
